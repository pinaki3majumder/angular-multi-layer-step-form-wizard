import { Routes } from '@angular/router';
import { MultiStepFormComponent } from './multi-step-form/multi-step-form.component';

export const routes: Routes = [
    { path: '', component: MultiStepFormComponent }
];
